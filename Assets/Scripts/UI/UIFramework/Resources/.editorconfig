# 顶级的 EditorConfig 文件
root = true

# 所有 C# 文件使用 UTF-8 编码
[*.cs]
charset = utf-8
indent_style = space
indent_size = 4

# 统一使用 LF 换行符 (跨平台兼容)
[*]
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true